# connection URL for jumprun database
db.url=jdbc:sqlserver://<host>:<port>;databaseName=<db name>;user=<db user>;password=<db password>

# users to skip during export
user.skip=

# manifested, but not to be listed as jumps (landing with plane, tandem student, pax)
price_item.nojump=

# upload URL
upload.url=http://thewebserver

# tmp dir (defaults to java.io.tmpdir)
jrsync.tmpdir=
